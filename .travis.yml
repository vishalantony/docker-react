language: generic
sudo: required
services:
    - docker

before_install:
    - docker build -t viantony/docker-react-test -f Dockerfile.dev .

script:
    -  docker run -e CI=true viantony/docker-react yarn test

